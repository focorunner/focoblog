<li>
  <%= gravatar_for user, size: 30 %>
  <%= link_to user.name, user %>
  <%= "(Administrator)" if user.admin %>
  <% if current_user.admin? && !user.admin && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "Delete User... Are you sure?" } %>
  <% end %>
</li>