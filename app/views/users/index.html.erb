<% provide(:title, 'All users') %>
<section class="row">
  <h1>All users</h1>

    <%= will_paginate %>

      <ul class="users" class="list-unstyled">
        <%= render @users %>
      </ul>

    <%= will_paginate %>
</section>